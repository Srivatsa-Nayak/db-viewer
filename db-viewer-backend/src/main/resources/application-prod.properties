# ==========================================
# Production Profile Overrides
# ==========================================

# Disable detailed SQL logging in prod
logging.level.org.springframework.jdbc=WARN
spring.jpa.show-sql=false

# Swagger (optionally disable in prod)
springdoc.swagger-ui.enabled=true
